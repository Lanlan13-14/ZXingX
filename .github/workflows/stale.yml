name: Stale

# Optional. Disabled by default for this fork.
on:
  workflow_dispatch:

jobs:
  noop:
    name: Stale noop
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo "Stale issue automation is not configured for this fork."
